# Cursor QA skills (iO)

This repo includes rules and skills for running full QA via Cursor (including Cloud Agent).

- **Rule:** `rules/qa-report-format.mdc` — QA report structure, severity, fix list format.
- **Skills:** `skills/qa-run`, `qa-frontend`, `qa-backend`, `qa-e2e`, `qa-merge-report`.

Use the full QA prompt (see repo or iO docs) to run a full QA; the Cloud Agent will use these when working on this repo.
